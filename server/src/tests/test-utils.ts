// import { Mongoose } from 'mongoose';

// // src/__mocks__/mongoose.ts
// const mongoose: Mongoose = jest.createMockFromModule('mongoose');

// let connectMock = jest.fn().mockResolvedValue(undefined);
// let disconnectMock = jest.fn().mockResolvedValue(undefined);
// let modelMock = jest.fn().mockReturnValue({
//     find: jest.fn().mockResolvedValue([]),
//     findById: jest.fn().mockResolvedValue(null),
//     create: jest.fn().mockResolvedValue({ _id: 'some_id' }),
// });

// mongoose.connect = connectMock;
// mongoose.disconnect = disconnectMock;
// mongoose.model = modelMock;

// export default mongoose;
